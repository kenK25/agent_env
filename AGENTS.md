# AGENTS.md — 共通エージェント規約

このファイルは **Claude Code と OpenAI Codex の共通ルール（Single Source of Truth）** です。
- Codex は `AGENTS.md` をネイティブに読み込みます。
- Claude Code は `CLAUDE.md` 経由で本ファイルを import します。
エージェント固有の指示は `CLAUDE.md`（Claude）にのみ追記し、共通ルールはここに書いてください。

## 1. 役割分担（デフォルト）

| 役割 | 担当 | 主な場所 |
|---|---|---|
| 設計・実装（主担当） | Claude Code | ローカル / Claude Code on the web / `@claude` |
| 独立レビュー（セカンドオピニオン） | Codex | PR 自動レビュー（GitHub Actions）/ `scripts/codex-review.sh` |
| 並列実装（切り出したタスク） | Codex | Codex cloud / `scripts/codex-exec.sh` |

- **実装者と最終レビュアーは別エージェントにする**（同一モデルの自己レビューで見落としが相関するのを避けるため）。
- 役割はタスク単位で入れ替えてよい。入れ替える場合は handoff ノートに明記する。

## 2. ブランチ規約

- Claude の作業ブランチ: `claude/<topic>`
- Codex の作業ブランチ: `codex/<topic>`
- 同一ブランチに両エージェントが同時にコミットしない。引き継ぐ場合は handoff ノートを残してから。
- `main` への直接 push 禁止。必ず PR 経由。マージは人間が判断する。

## 3. 引き継ぎ（handoff）プロトコル

エージェント間でタスクを渡すときは `.agents/handoff/<YYYYMMDD>-<topic>.md` を
`.agents/handoff/TEMPLATE.md` に沿って作成し、同じコミットに含める。
受け取った側は、作業開始時にまず該当ノートを読み、完了時に「結果」欄を追記する。

## 4. 作業ルール（両エージェント共通）

- 変更前に関連コードを読む。推測で API・設定値を書かない。外部仕様は一次情報（公式ドキュメント/リポジトリ）で確認し、確認できないものは「推定」と明記する。
- 変更は依頼範囲に限定する。無関係なリファクタや「ついで修正」はしない。
- コミット前に、リポジトリにある lint / test を実行する（`scripts/check.sh` があればそれを使う）。
- 秘密情報（API キー、トークン、`.env`）をコミットしない。ログやレビューコメントにも出さない。
- テストの skip・無効化で CI を通さない。

## 5. レビュー観点（Codex レビュー・Claude レビュー共通）

1. 正しさ（バグ、境界条件、エラー処理）
2. セキュリティ（インジェクション、権限、秘密情報の露出）
3. 依頼範囲からの逸脱
4. テストの有無と妥当性

指摘は「重大度（blocker / major / minor / nit）」「該当箇所（file:line）」「根拠」をセットで書く。

## 6. コミットメッセージ

- 1 行目: 命令形で要約（日本語・英語どちらでも可）
- 本文: 「なぜ」を書く
- 作業したエージェントが分かるよう、各ツール既定の Co-Authored-By トレーラーを残す
